# My Node App

A simple Node.js web application.

## Installation

1. Clone the repo: `git clone https://github.com/yourusername/my-node-app.git`
2. Install dependencies: `npm install`
3. Start the server: `npm start`
